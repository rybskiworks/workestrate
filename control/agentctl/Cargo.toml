[package]
name = "agentctl"
version = "0.1.0"
edition = "2021"
description = "Control plane CLI for the AI workestrator (Rust). See ../../README.md."
license = "MIT OR Apache-2.0"

[[bin]]
name = "agentctl"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
