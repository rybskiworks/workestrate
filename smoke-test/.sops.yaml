# Replace age1PLACEHOLDER... with the public recipient generated by:
#   age-keygen -y ~/.config/sops/age/ai-workbench-secrets.txt
# The private key must stay outside this repository.
keys:
  - &repo_secrets_v1 age1m0nngcd3jfcpjdl09vszhscvfe73x7kzlk30l2vjc9kvwxlrg5msefyrgt

creation_rules:
  - path_regex: ^\.env\.enc$
    input_type: dotenv
    output_type: dotenv
    key_groups:
      - age:
          - *repo_secrets_v1
