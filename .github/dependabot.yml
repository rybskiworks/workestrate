# Source, compiler and runtime pins are reviewed together, not advanced independently.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      compatible-actions:
        patterns: ['*']
        update-types: [minor, patch]
