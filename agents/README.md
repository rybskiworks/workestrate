# Agents

This directory holds optional mutable local checkouts of the agent forks.

- `agents/pi/` — local checkout of `github:georgrybski/pi`
- `agents/odysseus/` — local checkout of `github:georgrybski/odysseus`

Both are gitignored. The reproducible versions are pinned as flake inputs.

Launch profiles live under `profiles/agents/`.
