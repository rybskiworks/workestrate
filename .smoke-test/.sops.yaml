# Replace age1PLACEHOLDER... with the public recipient generated by:
#   age-keygen -y ~/.config/sops/age/ai-workbench-secrets.txt
# The private key must stay outside this repository.
keys:
  - &repo_secrets_v1 age13r2kva8vtkeps6kx4fqu2m2qmxfkzxc7rjk3284lt55cw8kk396stwuh39

creation_rules:
  - path_regex: ^\.env\.enc$
    input_type: dotenv
    output_type: dotenv
    key_groups:
      - age:
          - *repo_secrets_v1
